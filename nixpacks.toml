[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "if [ \"$EASYFLOW_PROCESS\" = \"worker\" ]; then exec npm run worker; else exec npm run start; fi"
port = 80
